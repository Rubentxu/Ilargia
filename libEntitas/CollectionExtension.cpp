#include "CollectionExtension.h"
#include "Entity.h"


namespace Entitas {

/*	std::shared_ptr<Entity> CollectionExtension::SingleEntity(const std::shared_ptr<ICollection<Entity> > &collection) {
		if(collection->Count != 1)
			throw std::exception(std::string(_TEXT("Expected exactly one entity but found ")) + std::to_string(collection->Count));

		return System::Linq::Enumerable::First(collection);
	}*/
}
